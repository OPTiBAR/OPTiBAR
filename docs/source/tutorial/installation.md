Installation
============

Use the following command to install the package.
```bash
pip install git+https://github.com/OPTiBAR/OPTiBAR
```
run the following command to ensure that the package is installed succesfuly.
